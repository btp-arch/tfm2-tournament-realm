export default function MatchRoomPage({ params }: { params: { id: string } }) {
  return <><h1>Match Room</h1><p className="muted">Placeholder for match {params.id}.</p><div className="card">Future match rooms will track check-in, random host assignment, side choice, reports, evidence, confirmations, and disputes.</div></>;
}
